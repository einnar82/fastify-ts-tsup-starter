export async function resetTestDatabase() {
    // Example: seed in-memory or clean database
    // Replace with actual DB code (e.g. truncate, seed, reset)
    console.log('🔄 Resetting test DB (mock)');
  }
  